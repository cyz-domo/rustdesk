name: Build Windows & Linux (AMD64 & ARM64)

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Build Target'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - windows-x64
          - windows-arm64
          - linux-x64
          - linux-arm64

env:
  RUST_VERSION: "1.75"
  SCITER_RUST_VERSION: "1.75"
  FLUTTER_VERSION: "3.24.5"
  FLUTTER_WINDOWS_ARM_VERSION: "3.44.8"
  LLVM_VERSION: "15.0.6"
  VCPKG_COMMIT_ID: "9e593bb18ea69cc5095e012465dcd675a822ed0d"
  VCPKG_CMAKE_VERSION: "3.31.5"

jobs:
  generate-bridge:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install prerequisites
        run: |
          sudo apt-get update -y
          sudo apt-get install -y clang cmake curl gcc git g++ libclang-dev libgtk-3-dev llvm-dev nasm ninja-build pkg-config wget

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version: "3.24.5"
          cache: true

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.RUST_VERSION }}
          components: "rustfmt"

      - name: Install bridge tools
        run: |
          cargo install cargo-expand --version 1.0.95 --locked
          cargo install flutter_rust_bridge_codegen --version 1.80.1 --features "uuid" --locked

      - name: Generate bridge
        run: |
          pushd flutter
          flutter pub get
          popd
          python3 ./res/inline-sciter.py
          ~/.cargo/bin/flutter_rust_bridge_codegen --rust-input ./src/flutter_ffi.rs --dart-output ./flutter/lib/generated_bridge.dart
          git status -s

      - name: Upload bridge artifact
        uses: actions/upload-artifact@v4
        with:
          name: bridge-artifact
          path: |
            flutter/lib/generated_bridge.dart
            flutter/lib/generated_bridge.freezed.dart
            src/bridge_generated.rs
            src/bridge_generated.io.rs

  build-windows:
    needs: [generate-bridge]
    name: build-windows-${{ matrix.job.arch }}
    runs-on: ${{ matrix.job.os }}
    strategy:
      fail-fast: false
      matrix:
        job:
          - {
              arch: x86_64,
              os: windows-2022,
              target: x86_64-pc-windows-msvc,
              flutter-arch: x64,
              vcpkg-triplet: x64-windows-static,
              build-args: "--vram"
            }
          - {
              arch: aarch64,
              os: windows-11-arm,
              target: aarch64-pc-windows-msvc,
              flutter-arch: arm64,
              vcpkg-triplet: arm64-windows-static,
              build-args: ""
            }
    steps:
      - name: Check target selection
        id: check_selection
        run: |
          $sel = "${{ inputs.target }}"
          $arch = "${{ matrix.job.arch }}"
          if ($sel -eq 'all' -or ($sel -eq 'windows-x64' -and $arch -eq 'x86_64') -or ($sel -eq 'windows-arm64' -and $arch -eq 'aarch64')) {
            echo "run_job=true" >> $env:GITHUB_OUTPUT
          } else {
            echo "run_job=false" >> $env:GITHUB_OUTPUT
          }

      - name: Checkout source code
        if: steps.check_selection.outputs.run_job == 'true'
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Restore bridge files
        if: steps.check_selection.outputs.run_job == 'true'
        uses: actions/download-artifact@v4
        with:
          name: bridge-artifact
          path: ./

      - name: Install LLVM and Clang
        if: steps.check_selection.outputs.run_job == 'true'
        uses: KyleMayes/install-llvm-action@v2
        with:
          version: ${{ env.LLVM_VERSION }}

      - name: Install Flutter
        if: steps.check_selection.outputs.run_job == 'true'
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version: ${{ env.FLUTTER_VERSION }}
          architecture: x64

      - name: Replace engine with rustdesk custom flutter engine
        if: steps.check_selection.outputs.run_job == 'true' && matrix.job.arch == 'x86_64'
        run: |
          flutter doctor -v
          flutter precache --windows
          Invoke-WebRequest -Uri https://github.com/rustdesk/engine/releases/download/main/windows-x64-release.zip -OutFile windows-x64-release.zip
          Expand-Archive -Path windows-x64-release.zip -DestinationPath windows-x64-release
          $flutterBin = Split-Path -Parent (Get-Command flutter).Source
          $enginePath = Join-Path $flutterBin "cache\artifacts\engine\windows-x64-release"
          New-Item -ItemType Directory -Force -Path $enginePath | Out-Null
          Copy-Item -Force -Recurse windows-x64-release\* $enginePath

      - name: Install Rust toolchain
        if: steps.check_selection.outputs.run_job == 'true'
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.SCITER_RUST_VERSION }}
          targets: ${{ matrix.job.target }}
          components: "rustfmt"

      - name: Setup vcpkg
        if: steps.check_selection.outputs.run_job == 'true'
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: C:\vcpkg
          vcpkgGitCommitId: ${{ env.VCPKG_COMMIT_ID }}
          doNotCache: false

      - name: Install vcpkg dependencies
        if: steps.check_selection.outputs.run_job == 'true'
        env:
          VCPKG_DEFAULT_HOST_TRIPLET: ${{ matrix.job.vcpkg-triplet }}
        shell: bash
        run: |
          "$VCPKG_ROOT/vcpkg" install --triplet ${{ matrix.job.vcpkg-triplet }} --x-install-root="$VCPKG_ROOT/installed"

      - name: Build rustdesk
        if: steps.check_selection.outputs.run_job == 'true'
        run: |
          python3 .\build.py --portable --flutter --skip-portable-pack --hwcodec ${{ matrix.job.build-args }}
          New-Item -ItemType Directory -Force -Path ./rustdesk | Out-Null
          Copy-Item -Force -Recurse "./flutter/build/windows/${{ matrix.job.flutter-arch }}/runner/Release/*" ./rustdesk/

      - name: Build self-extracted executable
        if: steps.check_selection.outputs.run_job == 'true'
        shell: bash
        run: |
          sed -i '/dpiAware/d' res/manifest.xml
          pushd ./libs/portable
          pip3 install -r requirements.txt
          python3 ./generate.py -f ../../rustdesk/ -o . -e ../../rustdesk/rustdesk.exe
          popd
          mkdir -p ./SignOutput
          if [ -f "./target/release/rustdesk-portable-packer.exe" ]; then
            cp -f ./target/release/rustdesk-portable-packer.exe ./SignOutput/rustdesk-${{ matrix.job.arch }}.exe
          elif [ -f "./libs/portable/target/release/rustdesk-portable-packer.exe" ]; then
            cp -f ./libs/portable/target/release/rustdesk-portable-packer.exe ./SignOutput/rustdesk-${{ matrix.job.arch }}.exe
          fi

      - name: Upload Windows Self-Extract Executable
        if: steps.check_selection.outputs.run_job == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-windows-${{ matrix.job.arch }}-exe
          path: ./SignOutput/rustdesk-${{ matrix.job.arch }}.exe
          if-no-files-found: warn

      - name: Upload Windows Release Folder
        if: steps.check_selection.outputs.run_job == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-windows-${{ matrix.job.arch }}-portable-folder
          path: ./rustdesk

  build-linux:
    needs: [generate-bridge]
    name: build-linux-${{ matrix.job.arch }}
    runs-on: ${{ matrix.job.on }}
    strategy:
      fail-fast: false
      matrix:
        job:
          - {
              arch: x86_64,
              target: x86_64-unknown-linux-gnu,
              distro: ubuntu18.04,
              on: ubuntu-22.04,
              deb_arch: amd64,
              vcpkg-triplet: x64-linux,
            }
          - {
              arch: aarch64,
              target: aarch64-unknown-linux-gnu,
              distro: ubuntu18.04,
              on: ubuntu-22.04-arm,
              deb_arch: arm64,
              vcpkg-triplet: arm64-linux,
            }
    steps:
      - name: Check target selection
        id: check_selection
        run: |
          sel="${{ inputs.target }}"
          arch="${{ matrix.job.arch }}"
          if [[ "$sel" == "all" || ("$sel" == "linux-x64" && "$arch" == "x86_64") || ("$sel" == "linux-arm64" && "$arch" == "aarch64") ]]; then
            echo "run_job=true" >> $GITHUB_OUTPUT
          else
            echo "run_job=false" >> $GITHUB_OUTPUT
          fi

      - name: Maximize build space
        if: steps.check_selection.outputs.run_job == 'true'
        run: |
          sudo rm -rf /opt/ghc /usr/local/lib/android /usr/share/dotnet || true
          sudo apt-get update -y
          sudo apt-get install -y nasm

      - name: Checkout source code
        if: steps.check_selection.outputs.run_job == 'true'
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust toolchain
        if: steps.check_selection.outputs.run_job == 'true'
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.RUST_VERSION }}
          targets: ${{ matrix.job.target }}

      - name: Restore bridge files
        if: steps.check_selection.outputs.run_job == 'true'
        uses: actions/download-artifact@v4
        with:
          name: bridge-artifact
          path: ./

      - name: Setup vcpkg
        if: steps.check_selection.outputs.run_job == 'true'
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: /opt/artifacts/vcpkg
          vcpkgGitCommitId: ${{ env.VCPKG_COMMIT_ID }}
          doNotCache: false

      - name: Install vcpkg dependencies
        if: steps.check_selection.outputs.run_job == 'true'
        run: |
          sudo apt install -y libva-dev
          "$VCPKG_ROOT/vcpkg" install --triplet ${{ matrix.job.vcpkg-triplet }} --x-install-root="$VCPKG_ROOT/installed"

      - uses: rustdesk-org/run-on-arch-action@d3fcfbb632b84cf7f6bc772bfaaa2c2f4f8789a8
        name: Build rustdesk in docker
        id: docker_build
        if: steps.check_selection.outputs.run_job == 'true'
        with:
          arch: ${{ matrix.job.arch }}
          distro: ${{ matrix.job.distro }}
          githubToken: ${{ github.token }}
          setup: |
            ls -l "${PWD}"
            ls -l /opt/artifacts/vcpkg/installed
          dockerRunArgs: |
            --volume "${PWD}:/workspace"
            --volume "/opt/artifacts:/opt/artifacts"
          shell: /bin/bash
          install: |
            apt-get update -y
            apt-get install -y build-essential clang cmake curl gcc git g++ libayatana-appindicator3-dev libasound2-dev libclang-10-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev libgtk-3-dev libpulse-dev libva-dev libxcb-randr0-dev libxcb-shape0-dev libxcb-xfixes0-dev libxdo-dev libxfixes-dev llvm-10-dev nasm ninja-build pkg-config python3 rpm unzip wget xz-utils libssl-dev
          run: |
            git config --global --add safe.directory "*"
            pushd /opt
            wget -O rust.tar.gz https://static.rust-lang.org/dist/rust-1.75.0-${{ matrix.job.target }}.tar.gz
            tar -zxvf rust.tar.gz > /dev/null && rm rust.tar.gz
            cd rust-1.75.0-${{ matrix.job.target }} && ./install.sh
            rm -rf rust-1.75.0-${{ matrix.job.target }}
            popd

            pushd /workspace
            export VCPKG_ROOT=/opt/artifacts/vcpkg
            cargo build --locked --lib --features hwcodec,flutter,unix-file-copy-paste --release

            pushd /opt
            wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_${{ env.FLUTTER_VERSION }}-stable.tar.xz
            tar xf flutter_linux_${{ env.FLUTTER_VERSION }}-stable.tar.xz
            export PATH=/opt/flutter/bin:$PATH
            popd

            pushd /workspace
            export DEB_ARCH=${{ matrix.job.deb_arch }}
            python3 ./build.py --flutter --skip-cargo
            mv rustdesk*.deb rustdesk-${{ matrix.job.arch }}.deb
            popd

      - name: Upload Linux DEB
        if: steps.check_selection.outputs.run_job == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-linux-${{ matrix.job.arch }}-deb
          path: rustdesk-${{ matrix.job.arch }}.deb
